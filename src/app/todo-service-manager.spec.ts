import { TodoServiceManager } from './todo-service-manager';

describe('TodoServiceManager', () => {
  it('should create an instance', () => {
    expect(new TodoServiceManager()).toBeTruthy();
  });
});
